import React from "react";
import ReactDOM from "react-dom/client";
import MonToFunFinal from "./MonoToFunMerge";
const root = ReactDOM.createRoot(document.getElementById("sample"));
function render() {
  root.render(
    <main>
      <MonToFunFinal />
    </main>
  );
}
render();
